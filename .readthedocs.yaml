# ENTROPIA ReadTheDocs Configuration
# Version: 2

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  jobs:
    pre_build:
      - pip install -e ".[docs]"
      - sphinx-build -b html docs/source docs/build/html -W

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true

formats:
  - epub
  - pdf
  - htmlzip

submodules:
  include: all
  recursive: true

search:
  ranking:
    src/entropia/core/*: 10
    src/entropia/simulation/*: 8
    src/entropia/dashboard/*: 7
    docs/source/tutorials/*: 5
